package com.example.bodhak.ir.declaration;

/**
 * Categorizes declaration roles for variables (field, parameter, local, record component, constant).
 */
public enum VariableKind {
    FIELD,
    PARAMETER,
    LOCAL,
    RECORD_COMPONENT,
    CONST
}
