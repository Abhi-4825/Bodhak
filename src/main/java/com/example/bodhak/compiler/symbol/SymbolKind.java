package com.example.bodhak.compiler.symbol;

/**
 * Enum of all types of symbols resolved by the compiler.
 */
public enum SymbolKind {
    NAMESPACE, MODULE, INTERFACE, CLASS, STRUCT, TRAIT, PROTOCOL, 
    METHOD, FUNCTION, FIELD, PROPERTY, CONSTRUCTOR, PARAMETER, VARIABLE
}
