package com.example.anuviya.ir.expression;

/**
 * Represents type parameter variance bounds (covariant, contravariant, invariant).
 */
public enum Variance {
    COVARIANT,
    CONTRAVARIANT,
    INVARIANT,
    BUST
}
